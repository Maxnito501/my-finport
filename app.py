# -*- coding: utf-8 -*-
import streamlit as st
import pandas as pd
import plotly.express as px
from datetime import datetime

# --- การตั้งค่าหน้าเว็บ ---
st.set_page_config(
    page_title="FinPort - ระบบจัดการพอร์ตลงทุนระดับโปร",
    page_icon=":bank:",
    layout="wide"
)

# --- ฟังก์ชันคำนวณ ---
def calculate_wealth(assets_df, liabs_df):
    t_a = assets_df["มูลค่า"].sum()
    t_l = liabs_df["มูลค่า"].sum()
    return t_a, t_l, t_a - t_l

# --- เริ่มต้นข้อมูล (Mock Data สำหรับการเปิดแอปครั้งแรก) ---
if 'assets' not in st.session_state:
    st.session_state.assets = pd.DataFrame([
        {"รายการ": "เงินฝากออมทรัพย์", "มูลค่า": 50000, "หมวดหมู่": "เงินสด/เงินฝาก"},
        {"รายการ": "กบข. (ส่วนสะสม+สมทบ)", "มูลค่า": 350000, "หมวดหมู่": "กบข./กองทุนสำรองฯ"},
        {"รายการ": "หุ้นสหกรณ์", "มูลค่า": 500000, "หมวดหมู่": "หุ้นกู้/หุ้นสหกรณ์"},
        {"รายการ": "RMF หุ้นโลก", "มูลค่า": 150000, "หมวดหมู่": "กองทุนลดหย่อนภาษี (RMF/SSF)"},
        {"รายการ": "กองทุนดัชนี SET50", "มูลค่า": 120000, "หมวดหมู่": "หุ้น/กองทุนดัชนี"},
        {"รายการ": "หุ้นปันผล (DCA)", "มูลค่า": 250000, "หมวดหมู่": "หุ้นปันผล"},
        {"รายการ": "หุ้นซิ่ง (เก็งกำไร)", "มูลค่า": 80000, "หมวดหมู่": "หุ้นเติบโต/หุ้นซิ่ง"}
    ])

if 'liabilities' not in st.session_state:
    st.session_state.liabilities = pd.DataFrame([
        {"รายการ": "หนี้บัตรเครดิต", "มูลค่า": 5000, "หมวดหมู่": "หนี้สินระยะสั้น"}
    ])

# รายการหมวดหมู่สินทรัพย์
ASSET_CATEGORIES = [
    "เงินสด/เงินฝาก", 
    "กบข./กองทุนสำรองฯ",
    "หุ้นกู้/หุ้นสหกรณ์", 
    "กองทุนลดหย่อนภาษี (RMF/SSF)",
    "หุ้น/กองทุนดัชนี", 
    "หุ้นปันผล", 
    "หุ้นเติบโต/หุ้นซิ่ง",
    "สินทรัพย์ทางเลือก (ทองคำ/คริปโต)"
]
LIAB_CATEGORIES = ["หนี้สินระยะสั้น", "หนี้สินระยะยาว"]

# คำนวณค่าปัจจุบัน
total_a, total_l, net_w = calculate_wealth(st.session_state.assets, st.session_state.liabilities)

# --- ส่วนแสดงผลหน้าเว็บหลัก ---
st.title("🏯 FinPort: ระบบจัดการพอร์ตลงทุนและหุ้นสหกรณ์")
st.write(f"อัปเดตสถานะความมั่งคั่ง: {datetime.now().strftime('%d/%m/%Y')}")

# แถบสรุปตัวเลข (Metric Cards)
c1, c2, c3 = st.columns(3)
with c1:
    st.metric("สินทรัพย์รวม (Assets)", f"{total_a:,.0f} ฿")
with c2:
    st.metric("หนี้สินรวม (Liabilities)", f"{total_l:,.0f} ฿", delta_color="inverse")
with c3:
    st.metric("ความมั่งคั่งสุทธิ (Net Worth)", f"{net_w:,.0f} ฿")

st.divider()

# การแสดงผลกราฟและฟอร์มรับค่า
left_col, right_col = st.columns([2, 1])

with left_col:
    st.subheader("📊 สัดส่วนการลงทุนแยกตามประเภท")
    fig = px.pie(
        st.session_state.assets, 
        values='มูลค่า', 
        names='หมวดหมู่', 
        hole=0.4,
        color_discrete_sequence=px.colors.qualitative.Pastel
    )
    fig.update_traces(textposition='inside', textinfo='percent+label')
    st.plotly_chart(fig, use_container_width=True)

with right_col:
    st.subheader("➕ เพิ่มรายการใหม่")
    with st.form("add_item_form"):
        target = st.radio("เลือกประเภท", ["สินทรัพย์", "หนี้สิน"], horizontal=True)
        name = st.text_input("ชื่อรายการ (เช่น หุ้นสหกรณ์, กบข., หุ้นซิ่ง)")
        val = st.number_input("มูลค่าปัจจุบัน (บาท)", min_value=0, step=1000)
        
        if target == "สินทรัพย์":
            cat = st.selectbox("เลือกหมวดหมู่สินทรัพย์", ASSET_CATEGORIES)
        else:
            cat = st.selectbox("เลือกหมวดหมู่หนี้สิน", LIAB_CATEGORIES)
            
        if st.form_submit_button("เพิ่มเข้าพอร์ต"):
            if name:
                new_data = pd.DataFrame([{"รายการ": name, "มูลค่า": val, "หมวดหมู่": cat}])
                if target == "สินทรัพย์":
                    st.session_state.assets = pd.concat([st.session_state.assets, new_data], ignore_index=True)
                else:
                    st.session_state.liabilities = pd.concat([st.session_state.liabilities, new_data], ignore_index=True)
                st.success("บันทึกข้อมูลเรียบร้อย!")
                st.rerun()
            else:
                st.error("กรุณาระบุชื่อรายการ")

# ส่วนตารางสรุปรายรายการ
st.subheader("📋 รายละเอียดพอร์ตลงทุนรายรายการ")
tab1, tab2 = st.tabs(["💰 รายการสินทรัพย์ทั้งหมด", "💳 รายการหนี้สินทั้งหมด"])
with tab1:
    st.dataframe(st.session_state.assets, use_container_width=True)
with tab2:
    st.dataframe(st.session_state.liabilities, use_container_width=True)

# --- แถบ Sidebar สำหรับวิเคราะห์ความเสี่ยง ---
st.sidebar.title("📈 วิเคราะห์ความเสี่ยง")

# กำหนดกลุ่มความเสี่ยงตามหมวดหมู่
high_risk_cats = ["หุ้นเติบโต/หุ้นซิ่ง", "หุ้น/กองทุนดัชนี", "หุ้นปันผล", "สินทรัพย์ทางเลือก (ทองคำ/คริปโต)"]
low_risk_cats = ["เงินสด/เงินฝาก", "กบข./กองทุนสำรองฯ", "หุ้นกู้/หุ้นสหกรณ์", "กองทุนลดหย่อนภาษี (RMF/SSF)"]

risk_high = st.session_state.assets[st.session_state.assets['หมวดหมู่'].isin(high_risk_cats)]['มูลค่า'].sum()
risk_low = st.session_state.assets[st.session_state.assets['หมวดหมู่'].isin(low_risk_cats)]['มูลค่า'].sum()

total_val = risk_high + risk_low
if total_val > 0:
    st.sidebar.write(f"สินทรัพย์เสี่ยง (High Risk): **{risk_high:,.0f} ฿**")
    st.sidebar.write(f"สินทรัพย์มั่นคง (Low Risk): **{risk_low:,.0f} ฿**")
    
    high_pct = (risk_high / total_val) * 100
    st.sidebar.progress(risk_high / total_val)
    st.sidebar.caption(f"สัดส่วนความเสี่ยงปัจจุบัน: เสี่ยง {high_pct:.1f}% / มั่นคง {100-high_pct:.1f}%")
    
    if high_pct > 70:
        st.sidebar.warning("พอร์ตของคุณมีความเสี่ยงสูงมาก! (เน้นการเติบโต)")
    elif high_pct < 30:
        st.sidebar.info("พอร์ตของคุณเน้นความมั่นคงเป็นหลัก (Low Risk Heavy)")
    else:
        st.sidebar.success("พอร์ตของคุณมีความสมดุลระหว่างความเสี่ยงและความมั่นคง")

st.sidebar.divider()
st.sidebar.caption("หมายเหตุ: หุ้นสหกรณ์ถูกจัดอยู่ในกลุ่มสินทรัพย์มั่นคงเนื่องจากปันผลสม่ำเสมอ")
